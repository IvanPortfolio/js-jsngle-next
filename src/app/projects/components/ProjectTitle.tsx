export function ProjectTitle(props: { title: any }) {
    return (
        <h2 className="text-[28px] leading-[36.4px] text-dark dark:text-white font-bold">
            {props.title}
        </h2>
    );
}
